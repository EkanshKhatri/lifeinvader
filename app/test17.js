import { carsList } from './src/data.js'; let carAliasMap = []; carsList.forEach(car = let lowerCar = car.toLowerCase(); carAliasMap.push({ alias: lowerCar, fullName: car }); let noHyphen = lowerCar.replace(/-/g, ' '); if (noHyphen !== lowerCar) { carAliasMap.push({ alias: noHyphen, fullName: car }); } let cleanCar = noHyphen.replace(/[()]/g, ''); let words = cleanCar.split(' '); if (words.length  { let withoutMake = words.slice(1).join(' '); if (withoutMake.length  { carAliasMap.push({ alias: withoutMake, fullName: car }); } } }); console.log(carAliasMap.find(c = === 'Declasse Corvette C7'));  
